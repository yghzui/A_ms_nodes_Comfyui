# ComfyUI 自定义循环逻辑开发指南

本文档详细说明了如何利用 `MyBatchManager` 和 `batch_utils` 在 ComfyUI 中实现自动循环处理（Bypass DAG 限制），并对比了业界常见的实现方案。

## 1. 核心原理与方案对比

在 ComfyUI 中实现循环主要有三种方案，本项目最终采用方案 C：

### 方案 A：图展开 (Graph Expansion) —— ComfyUI-Easy-Use 采用
*   **原理**：节点在执行时利用 `GraphBuilder` 动态构建子图，并返回 `expand` 指令让 ComfyUI 引擎用新生成的子图替换当前节点。
*   **核心实现逻辑**：
    1.  **`forLoopStart` 节点**：
        *   初始化 `GraphBuilder`。
        *   在子图中创建 `easy whileLoopStart` 节点，传入初始索引（通常为 0）和总循环次数。
        *   返回 `{"expand": graph.finalize()}`，将自身替换为包含 `while` 循环开始逻辑的子图。
    2.  **`forLoopEnd` 节点**：
        *   初始化 `GraphBuilder`。
        *   通过 `dynprompt` 获取对应的 `forLoopStart` 节点信息（如总次数 `total`）。
        *   构建计数逻辑：创建 `easy mathInt` 节点将当前索引 +1。
        *   构建判断逻辑：创建 `easy compare` 节点判断 `当前索引 < 总次数`。
        *   创建 `easy whileLoopEnd` 节点，将新的索引和判断结果传回，闭合循环。
        *   同样返回 `{"expand": graph.finalize()}`。
*   **代码参考**：
    *   文件：`ComfyUI-Easy-Use/py/nodes/logic.py`
    *   关键类：`forLoopStart`, `forLoopEnd`
    *   关键方法：`graph.finalize()`
*   **优点**：进度条连续，原生支持子图逻辑，用户体验更像是一个完整的整体任务。
*   **缺点**：实现极为复杂，深度依赖 ComfyUI 内部的 `GraphBuilder` 和执行引擎的子图展开机制，维护成本高。

### 方案 B：空列表截断 (Empty List Blocking) —— ComfyUI-VideoHelperSuite (VHS) 采用
*   **原理**：返回空列表 `[]`。
*   **局限性**：仅适用于能够处理空输入的下游节点（如 VHS 自身的一些节点）。如果下游接了 `SaveImage` 等标准节点，**会报错** `IndexError: list index out of range`，因为这些节点不检查输入是否为空。

### 方案 C：执行阻断器 (ExecutionBlocker) —— **本项目推荐**
*   **原理**：返回 `comfy_execution.graph.ExecutionBlocker(None)` 对象。
*   **机制**：ComfyUI 的执行引擎 (`execution.py`) 在处理节点输入时，如果发现任何一个输入是 `ExecutionBlocker`，就会**静默跳过**该节点的执行，并继续向下传递 `ExecutionBlocker`。
*   **优点**：
    *   **静默停止**：不会像 `InterruptProcessingException` 那样显示红框报错。
    *   **安全**：不会像返回空列表那样导致下游节点因索引越界而崩溃。
    *   **逻辑清晰**：明确告知引擎“停止这条路径的执行”。

---

## 2. 核心文件

### `batch_utils.py` (工具库)
位于 `nodes/batch_utils.py`。
- **`requeue_workflow_unchecked()`**: 核心函数，用于触发下一次循环。它会复制当前运行的 Prompt，增加 `requeue` 计数，并将其重新推入 ComfyUI 的执行队列。

### `my_batch_manager.py` (管理器节点)
位于 `nodes/my_batch_manager.py`。
- **作用**：负责初始化状态。
- **逻辑**：如果是用户点击的首次运行（`requeue=0`），它会重置索引和结果；如果是自动重入队的运行，它会保持状态不变。

---

## 3. 如何让其他节点支持循环

要创建一个新的循环系统，你需要修改或创建三类节点。

### A. 修改输入节点 (Input Node)
*例如：读取数据的节点，如 `TextInputBatch` 或 `LoadImageBatch`。*

**职责：**
1.  计算任务总数并告知管理器。
2.  根据管理器的当前索引输出对应的数据项。

**代码示例：**
```python
@classmethod
def INPUT_TYPES(cls):
    return {
        "required": { ... },
        "optional": {
            "batch_manager": ("MY_BATCH_MANAGER",), # 添加管理器输入
        }
    }

def execute(self, ..., batch_manager=None):
    entries = [...] # 准备数据列表
    
    if batch_manager:
        batch_manager.total_count = len(entries) # 1. 告知总数
        current_index = batch_manager.current_index
        print(f"LoopMode: Processing {current_index}/{len(entries)}")
    else:
        current_index = index # 普通模式
        
    current_item = entries[current_index]
    
    return (current_item, ...)
```

### B. 修改中间节点 (Intermediate Node)
*例如：图像累积节点，如 `ImageBatchAccumulator`。*

**职责：**
1.  接收单次处理的结果并保存到 `batch_manager.results`。
2.  **循环未结束时**：触发重入队，并**返回 `ExecutionBlocker(None)`** 以静默停止下游节点。
3.  **循环结束时**：合并结果，返回最终数据。

**代码示例：**
```python
import torch
from .batch_utils import requeue_workflow_unchecked
try:
    from comfy_execution.graph import ExecutionBlocker
except ImportError:
    from comfy_execution.graph_utils import ExecutionBlocker

def accumulate(self, batch_manager, image):
    # 1. 初始化结果列表
    if not hasattr(batch_manager, "results") or not isinstance(batch_manager.results, list):
        batch_manager.results = []
        
    # 2. 收集结果
    batch_manager.results.append(image.clone())
    batch_manager.current_index += 1
    
    # 3. 判断循环状态
    if batch_manager.current_index < batch_manager.total_count:
        # === 循环未结束 ===
        print("Loop: Requeueing...")
        requeue_workflow_unchecked()
        
        # 【关键】返回 ExecutionBlocker(None)
        # ComfyUI 引擎检测到此对象后，会自动跳过下游节点的执行，且不报错
        return (ExecutionBlocker(None),)
    
    # === 循环结束 ===
    print("Loop: Batch complete.")
    batch_manager.is_running = False
    
    # 4. 合并结果
    try:
        final_batch = torch.cat(batch_manager.results, dim=0)
    except:
        final_batch = image
        
    # 5. 清理内存
    batch_manager.results = []
    
    return (final_batch,)
```

### C. 修改输出节点 (Output Node)
*例如：显示结果节点，如 `ShowResultLast`。*

**职责：**
在循环未完成时，同样返回 `ExecutionBlocker`。

**代码示例：**
```python
from .batch_utils import requeue_workflow_unchecked
try:
    from comfy_execution.graph import ExecutionBlocker
except ImportError:
    from comfy_execution.graph_utils import ExecutionBlocker

def execute(self, images, ..., batch_manager=None):
    if batch_manager:
        batch_manager.results.extend(images)
        batch_manager.current_index += 1
        
        if batch_manager.current_index < batch_manager.total_count:
            # === 循环未结束 ===
            requeue_workflow_unchecked()
            
            # 返回 Blocker，停止执行
            return {
                "ui": {"images": []},
                "result": (ExecutionBlocker(None),)
            }
        else:
            # === 循环结束 ===
            final_files = batch_manager.results
            batch_manager.is_running = False
            batch_manager.results = []
            
            return {"ui": {"images": final_files}}
            
    return {"ui": {"images": images}}
```
