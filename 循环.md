# ComfyUI 自定义循环逻辑开发指南

本文档详细说明了如何利用 `MyBatchManager` 和 `batch_utils` 在 ComfyUI 中实现自动循环处理（Bypass DAG 限制）。

## 1. 核心原理

ComfyUI 默认是 DAG（有向无环图）执行模式，不支持循环。我们通过以下方式实现循环：

1.  **状态保持**：使用 `MyBatchManager` 对象在内存中存储当前索引 (`current_index`)、总数 (`total_count`) 和结果列表 (`results`)。
2.  **自动重入队**：在处理完单次任务后，如果未达到总数，通过修改 Prompt 中的 `requeue` 参数并重新提交任务到队列。
3.  **优雅中断**：使用 `comfy.model_management.InterruptProcessingException` 在循环未完成时阻断下游节点的执行，避免产生无效输出或弹窗报错。

---

## 2. 核心文件

### `batch_utils.py` (工具库)
位于 `nodes/batch_utils.py`。
- **`MyBatchManagerObj`**: 状态容器类。
- **`requeue_workflow_unchecked()`**: 核心函数，用于触发下一次循环。它会复制当前运行的 Prompt，增加 `requeue` 计数，并将其重新推入 ComfyUI 的执行队列。

### `my_batch_manager.py` (管理器节点)
位于 `nodes/my_batch_manager.py`。
- **作用**：负责初始化状态。
- **逻辑**：如果是用户点击的首次运行（`requeue=0`），它会重置索引和结果；如果是自动重入队的运行，它会保持状态不变。

---

## 3. 如何让其他节点支持循环

要创建一个新的循环系统，你需要修改或创建三类节点：**输入节点**（生产者）、**中间节点**（收集者）和**输出节点**（消费者）。

### A. 修改输入节点 (Input Node)
*例如：读取数据的节点，如 `TextInputBatch` 或 `LoadImageBatch`。*

**职责：**
1.  计算任务总数并告知管理器。
2.  根据管理器的当前索引输出对应的数据项。

**代码示例：**
```python
@classmethod
def INPUT_TYPES(cls):
    return {
        "required": { ... },
        "optional": {
            # 添加管理器输入
            "batch_manager": ("MY_BATCH_MANAGER",),
        }
    }

def execute(self, ..., batch_manager=None):
    # 1. 准备数据列表
    entries = [...] 
    
    # 2. 接管索引逻辑
    if batch_manager:
        # 告诉管理器总共有多少项任务
        batch_manager.total_count = len(entries)
        # 使用管理器的当前索引
        current_index = batch_manager.current_index
        print(f"LoopMode: Processing {current_index}/{len(entries)}")
    else:
        # 普通模式：使用手动输入的索引
        current_index = index
        
    # 3. 获取当前项
    current_item = entries[current_index]
    
    return (current_item, ...)
```

### B. 修改中间节点 (Intermediate Node)
*例如：图像累积节点，如 `ImageBatchAccumulator`。*

**职责：**
1.  接收单次处理的结果并保存到 `batch_manager.results`。
2.  判断循环是否结束：
    - **未结束**：触发重入队，并抛出异常阻断下游。
    - **已结束**：合并结果，清空缓存，并返回最终数据给下游。

**代码示例：**
```python
import torch
import comfy.model_management
from .batch_utils import requeue_workflow_unchecked

def accumulate(self, batch_manager, image):
    # 1. 确保结果列表初始化
    if not hasattr(batch_manager, "results") or not isinstance(batch_manager.results, list):
        batch_manager.results = []
        
    # 2. 收集结果 (注意 clone 避免引用问题)
    batch_manager.results.append(image.clone())
    
    # 3. 增加索引
    batch_manager.current_index += 1
    
    # 4. 判断循环状态
    if batch_manager.current_index < batch_manager.total_count:
        # === 循环未结束 ===
        print("Loop: Requeueing...")
        requeue_workflow_unchecked()
        
        # 关键：抛出特定异常以优雅停止当前执行流
        # 这不会导致 UI 弹窗报错，只是静默停止后续节点
        raise comfy.model_management.InterruptProcessingException()
    
    else:
        # === 循环结束 ===
        print("Loop: Batch complete.")
        batch_manager.is_running = False
        
        # 5. 合并结果
        try:
            final_batch = torch.cat(batch_manager.results, dim=0)
        except:
            final_batch = image # Fallback
            
        # 6. 重要：清理内存！
        batch_manager.results = []
        
        return (final_batch,)
```

### C. 修改输出节点 (Output Node)
*例如：显示结果节点，如 `ShowResultLast`。*

**职责：**
与中间节点类似，但通常不再向下游传递数据，而是直接展示或保存。

**代码示例：**
```python
import comfy.model_management
from .batch_utils import requeue_workflow_unchecked

def execute(self, images, ..., batch_manager=None):
    # 处理当前的保存逻辑
    saved_files = save_images(images)
    
    if batch_manager:
        # 1. 收集结果
        batch_manager.results.extend(saved_files)
        batch_manager.current_index += 1
        
        if batch_manager.current_index < batch_manager.total_count:
            # === 循环未结束 ===
            requeue_workflow_unchecked()
            raise comfy.model_management.InterruptProcessingException()
        else:
            # === 循环结束 ===
            final_files = batch_manager.results
            
            # 重置状态并清理内存
            batch_manager.is_running = False
            batch_manager.results = []
            
            return {"ui": {"images": final_files}}
            
    return {"ui": {"images": saved_files}}
```

---

## 4. 关键注意事项

1.  **内存管理**：
    在循环结束时（`else` 分支），务必执行 `batch_manager.results = []`。如果不清空，下一次运行工作流时，旧的结果会残留在内存中，导致结果错误或内存溢出。

2.  **优雅中断**：
    始终使用 `raise comfy.model_management.InterruptProcessingException()` 来停止中间步骤的下游执行。不要使用 `return` 空值，因为下游节点可能无法处理空输入；也不要使用 `ValueError`，这会导致 UI 弹出红色错误框。

3.  **连接顺序**：
    确保 `MyBatchManager` 节点正确连接到了所有参与循环的节点（输入、中间、输出）。任何未连接管理器的节点将无法感知循环状态，只会执行一次。
