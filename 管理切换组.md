# 管理切换组设计文档

## 1. 文档目的

本文档用于定义“管理切换组”系统的完整设计方案，目标是在 ComfyUI 工作流中实现一套可维护、可扩展、兼容 UI 与 API 的多节点分组切换能力。

该系统不是单一节点，而是一套由以下部分组成的功能系统：

- 控制节点
- 前端组管理界面
- 后端分组存储接口
- 运行时 prompt 应用逻辑

本文档重点说明：

- 需求边界
- 可行性分析
- 系统拆分
- 多期实施计划
- MVP 范围
- 接口与数据结构设计
- 开发顺序与验收标准

---

## 2. 设计目标

### 2.1 总体目标

设计一个“工作流分组控制系统”，用于批量管理多个节点的输入值与运行状态，并支持按“组”为单位快速切换。

### 2.2 预期效果

用户可以预先定义多套工作流配置组，例如：

- 人像高细节组
- 快速预览组
- 低显存组
- 文生图组
- 图生图组

切换组后，系统可以一次性完成：

- 批量修改多个节点输入值
- 批量同步前端节点显示状态
- 在 API 模式下应用当前组的真实配置
- 后续阶段支持节点 normal / bypass / disabled 状态切换

### 2.3 设计约束

本系统必须满足以下原则：

- UI 模式与 API 模式语义一致
- 组配置必须可持久化保存
- 第一阶段优先保证稳定可用
- 不依赖临时前端状态作为唯一真实来源
- 对失效节点和异常项有可解释的处理策略

---

## 3. 需求理解

### 3.1 本质需求

你的真实需求不是“再做一个简单逻辑节点”，而是做一个“工作流预设组控制器”。

每个组本质上是一批规则的集合。每条规则对应一个目标节点，并定义：

- 改哪个输入项
- 改成什么值
- 是否控制该节点状态
- 是否参与当前组应用

### 3.2 用户希望具备的能力

#### 组管理能力

- 新建组
- 编辑组
- 删除组
- 重命名组
- 复制组
- 选择激活组
- 管理多个组

#### 组内容能力

- 一个组可包含多个节点配置项
- 每个配置项可指向不同节点
- 每个配置项可修改不同输入名
- 每个配置项可保存一个目标值
- 后续支持控制节点状态

#### 工作流控制能力

- 切换组后快速切换输出方案
- 减少手动逐个改节点参数
- 保持 UI 和 API 的运行结果一致

### 3.3 后续增强需求

后续目标包括但不限于：

- 节点 `normal`
- 节点 `bypass`
- 节点 `disabled`
- 一键覆盖组内容
- 失效项重绑定
- 导入导出分组

---

## 4. 现有基础与可复用能力

### 4.1 可直接参考的现有节点与模块

- [MultiInputStateMapper](file:///d:/AI/comfyui/ComfyUI_windows_my/ComfyUI/custom_nodes/A_my_nodes/nodes/multi_input_state_mapper.py)
- [GroupSwitchAny](file:///d:/AI/comfyui/ComfyUI_windows_my/ComfyUI/custom_nodes/A_my_nodes/nodes/group_switch_any.py)
- [GetNodeInputValue](file:///d:/AI/comfyui/ComfyUI_windows_my/ComfyUI/custom_nodes/A_my_nodes/nodes/load_ui_node_value.py)
- [routes.py](file:///d:/AI/comfyui/ComfyUI_windows_my/ComfyUI/custom_nodes/A_my_nodes/routes.py)

### 4.2 为什么这些基础有用

#### `MultiInputStateMapper`

说明项目已经支持以下模式：

- 动态输入
- 前端驱动输入扩展
- 后端逻辑计算

这意味着“动态组控制节点”在项目结构上是可行的。

#### `GroupSwitchAny`

说明项目已经有“按组切换”的基础概念，后续可以参考其分组思路，但新的系统需要比它更强：

- 它是输入分组切换
- 你的需求是工作流多节点配置组切换

#### `GetNodeInputValue`

说明项目已经建立了“通过 `node_id + input_name` 定位目标节点输入”的基础能力。

这是实现分组系统的关键基础之一。

#### `routes.py` 与现有资产管理 UI

说明项目已具备：

- JSON 持久化接口
- 前端管理界面模式
- 路由保存与读取能力

因此本系统不需要从零开始设计管理面板与存储方式。

---

## 5. 可行性分析

### 5.1 高可行部分

以下部分是高可行、适合优先落地的：

- 多节点值分组保存
- 多节点值分组切换
- 分组增删改查
- 前端 UI 同步显示
- API 模式运行时按组选值

### 5.2 中等复杂部分

以下部分可做，但需要更谨慎设计：

- 值类型兼容
- 失效节点标记
- prompt 运行时改写
- 缓存失效控制

### 5.3 高风险部分

以下部分不建议第一期硬上：

- 节点 `bypass`
- 节点 `disabled`
- 改连线
- 改节点拓扑
- 跨工作流自动重绑定

### 5.4 结论

这个想法是可行的，但最佳路线不是“一次做全”，而是“按阶段逐步实现”。

推荐顺序：

1. 先做多节点值分组切换 MVP
2. 再做节点状态控制
3. 再做稳定性与维护增强
4. 最后做高级体验与联动能力

---

## 6. 总体架构设计

### 6.1 系统组成

本系统建议分为四层：

#### 第一层：主控制节点

负责：

- 选择当前激活组
- 控制是否自动应用
- 输出当前组信息
- 在运行时触发组应用

#### 第二层：前端组管理器

负责：

- 浏览分组
- 新建/编辑/删除/复制组
- 选择目标节点与目标输入
- 保存当前值为组项
- 将组应用到当前画布

#### 第三层：后端路由与存储

负责：

- 组配置保存
- 组配置读取
- 基础结构校验
- 提供统一 JSON 接口

#### 第四层：运行时应用器

负责：

- 读取当前 prompt
- 定位组内目标节点
- 改写目标 `inputs`
- 在 API 模式下应用真实组选值

### 6.2 为什么不建议做成单一纯后端节点

因为你的需求中包含大量“管理类能力”：

- 新建组
- 编辑组
- 删除组
- 重命名组
- 浏览组内项目

这些都不适合塞进单个节点 widgets 中，必须有更完整的前端管理界面。

---

## 7. 核心设计原则

### 7.1 一致性原则

同一个组在以下场景中必须有尽量一致的含义：

- 编辑器内运行
- API 模式运行
- 重新打开工作流后再次运行

### 7.2 持久化原则

组配置必须保存到独立 JSON，不应只依赖节点临时 widget。

### 7.3 分阶段原则

第一阶段只做“值组切换”，不强行把状态控制一起完成。

### 7.4 可解释原则

目标节点不存在、输入名失效、类型不匹配时，系统必须能提示，而不是静默失效。

### 7.5 可维护原则

数据结构、接口和代码拆分必须考虑后续扩展，而不是只满足第一版临时可用。

---

## 8. 主控制节点设计

### 8.1 节点建议名

建议名称：

- `WorkflowGroupPresetManager`

建议分类：

- `A_my_nodes/workflow`

### 8.2 节点职责

主控制节点不负责“复杂编辑”，只负责：

- 选择当前组
- 告诉系统当前激活的是哪一组
- 控制运行时是否应用该组
- 输出组状态信息供其它节点或调试使用

### 8.3 参数建议

#### `group_name`

- 当前激活组名称

#### `auto_apply`

- 是否在运行时自动应用当前组

#### `apply_scope`

候选值：

- `values_only`
- `values_and_state`

第一期仅实现：

- `values_only`

#### `fallback_mode`

候选值：

- `ignore_missing`
- `warn_missing`
- `error_missing`

#### `sync_ui_preview`

- 是否同步更新前端节点显示值

#### `group_version`

- 只读显示，用于缓存与调试

#### `description`

- 只读说明文字

### 8.4 输出建议

- `group_name`
- `group_id`
- `applied_count`
- `missing_count`
- `status_text`

---

## 9. 数据结构设计

### 9.1 顶层结构

```json
{
  "version": 1,
  "groups": [
    {
      "id": "group_xxx",
      "name": "人像-高细节",
      "enabled": true,
      "version": 1,
      "items": []
    }
  ]
}
```

### 9.2 单个组结构

```json
{
  "id": "group_xxx",
  "name": "人像-高细节",
  "enabled": true,
  "version": 1,
  "items": [
    {
      "id": "item_xxx",
      "target_node_id": "123",
      "target_node_title": "KSampler",
      "target_class_type": "KSampler",
      "target_input_name": "steps",
      "value_type": "INT",
      "value": 28,
      "apply_mode": "set_widget_value",
      "node_state": "normal",
      "enabled": true,
      "note": ""
    }
  ]
}
```

### 9.3 字段说明

#### 组字段

- `id`
  - 组唯一 ID
- `name`
  - 组名
- `enabled`
  - 组是否启用
- `version`
  - 组版本号
- `items`
  - 该组下的规则列表

#### 组项字段

- `id`
  - 规则唯一 ID
- `target_node_id`
  - 当前工作流中的节点 ID
- `target_node_title`
  - 仅用于显示与辅助识别
- `target_class_type`
  - 节点类型，便于后续重绑定
- `target_input_name`
  - 目标输入项名称
- `value_type`
  - 值类型
- `value`
  - 写入目标输入的值
- `apply_mode`
  - 应用方式
- `node_state`
  - 节点状态
- `enabled`
  - 当前组项是否启用
- `note`
  - 备注

### 9.4 值类型建议

第一期建议支持：

- `INT`
- `FLOAT`
- `BOOLEAN`
- `STRING`
- `COMBO`
- `JSON_STRING`

第一期不建议支持：

- 图像张量
- 模型对象
- 潜空间对象
- 任意 Python 对象

原因：

- 不可稳定序列化
- 不利于 API 模式一致性
- 不适合作为通用预设项

---

## 10. 运行时应用逻辑

### 10.1 UI 模式应用流程

1. 用户在前端管理器或控制节点中选择一个组
2. 前端读取该组所有 `items`
3. 根据 `target_node_id` 定位目标节点
4. 根据 `target_input_name` 定位目标 widget
5. 将 `value` 写入目标 widget
6. 如需同步状态，则更新节点前端状态
7. 刷新画布与节点显示

### 10.2 API 模式应用流程

1. 控制节点获取当前激活组
2. 后端通过隐藏 `PROMPT` 获取当前实际执行 prompt
3. 从分组 JSON 中读取当前组配置
4. 遍历组项并改写目标节点 `inputs`
5. 使用改写后的 prompt 继续执行

### 10.3 设计意义

这样可以解决单纯靠前端状态带来的问题：

- UI 里看到的是一套值
- API 实际跑的是另一套旧值

本系统的核心就是避免这种不一致。

---

## 11. 节点状态控制设计

### 11.1 目标状态

后续预期支持：

- `normal`
- `bypass`
- `disabled`

### 11.2 难点

这部分复杂度明显高于普通值切换，主要原因：

- 前端节点状态不一定天然映射到后端执行状态
- bypass 是否成立与节点类型和连接关系有关
- disabled 可能导致整条链路不合法

### 11.3 设计建议

这部分必须单独分期实现：

- 先做前端状态切换
- 再研究运行时语义同步
- 再补异常处理与兼容性验证

### 11.4 结论

可以做，但不建议列入第一期 MVP。

---

## 12. 前端管理器设计

### 12.1 交互形态建议

建议采用以下任一方式：

- 独立面板
- 节点侧边抽屉
- 节点按钮唤起对话框

不建议把复杂的组管理界面塞进普通 widgets。

### 12.2 核心功能

- 浏览所有组
- 新建组
- 删除组
- 重命名组
- 复制组
- 编辑组项
- 搜索目标节点
- 搜索目标输入项
- 从当前节点读取值
- 一键应用当前组
- 一键保存当前画布值到组

### 12.3 可选增强

- 失效节点高亮
- 类型不匹配提示
- 组内条目排序
- 批量启用/禁用组项
- 组差异对比

---

## 13. 后端路由设计

### 13.1 推荐接口

- `GET /a_my_nodes/workflow_groups`
- `POST /a_my_nodes/workflow_groups`
- `GET /a_my_nodes/workflow_groups/schema`

### 13.2 存储文件建议

建议独立存储文件：

- `workflow_groups_db.json`

### 13.3 后端职责

- 读取组配置
- 保存组配置
- 返回 schema
- 做基础结构校验
- 保证 JSON 基本合法性

### 13.4 为什么不复用提示词/模型资产库 JSON

不建议直接混入现有资产管理 JSON，原因：

- 语义不同
- 数据结构不同
- 后期维护成本更高
- 容易污染已有功能边界

---

## 14. 失效与重绑定机制

### 14.1 常见失效原因

- 节点被删除
- 节点 ID 改变
- 输入名改变
- 节点类型改变
- 工作流结构大改

### 14.2 解决策略

系统应支持：

- 标记失效组项
- 显示失效原因
- 提供重绑定入口

### 14.3 推荐重绑定优先级

1. 按 `target_node_id`
2. 按 `target_class_type + target_node_title`
3. 由用户手动指定

### 14.4 实施阶段建议

重绑定机制不进入第 1 期，建议放到第 3 期。

---

## 15. 兼容性设计

### 15.1 UI 模式

- 前端应用组值
- 编辑器中同步显示结果
- 后端运行时可再次校正 prompt

### 15.2 API 模式

- 不依赖浏览器当前 widget 状态
- 只依赖控制节点、分组数据和当前 prompt
- 确保这次运行使用的是真实组选值

### 15.3 结论

UI 与 API 必须双通道同步，而不能只做前端。

---

## 16. 缓存与执行影响

### 16.1 问题

组切换会改变实际 prompt 内容，因此缓存逻辑必须考虑组变化。

### 16.2 建议方案

- 控制节点参与缓存 key
- 组版本号变化视为输入变化
- 激活组变化视为输入变化

### 16.3 目的

避免 ComfyUI 误复用旧结果，导致切组后输出不变。

---

## 17. 错误处理策略

### 17.1 推荐错误级别

- `ignore`
- `warn`
- `error`

### 17.2 典型异常场景

- 目标节点不存在
- 目标输入不存在
- 值类型不匹配
- 组配置格式异常
- 状态无法应用

### 17.3 默认建议

第一阶段默认使用：

- `warn`

原因：

- 不会因单个失效项阻塞整组应用
- 便于快速迭代和调试

---

## 18. 多期实施计划

### 第 1 期：多节点值分组切换 MVP

#### 目标

先把最有价值、最稳定的功能做出来。

#### 范围

- 分组 JSON 存储
- 前端组管理基础界面
- 选择多个节点输入保存为组
- 手动切换组并批量设置 widget 值
- 控制节点运行时按组改写 prompt
- 支持 UI 和 API

#### 不包含

- 节点状态控制
- 自动重绑定
- 导入导出
- 组差异对比

#### 产出物

- 新控制节点
- 新路由
- 新 JSON 存储
- 新前端基础管理 UI
- API 模式兼容

### 第 2 期：节点状态控制

#### 目标

支持节点：

- `normal`
- `bypass`
- `disabled`

#### 范围

- 前端 UI 中设置状态项
- 切组时前端应用节点状态
- 后端运行时补充状态同步逻辑
- 状态异常提示

#### 风险

- 节点类型差异带来兼容性问题
- bypass 语义在不同节点上可能不稳定

#### 产出物

- 节点状态控制能力
- 状态同步策略
- 状态失败提示

### 第 3 期：稳定性增强

#### 目标

让系统适合长期维护。

#### 范围

- 失效项扫描
- 自动重绑定辅助
- 组复制
- 组排序
- 导入导出 JSON
- 组版本管理
- 变更预览

#### 产出物

- 重绑定机制
- 导入导出能力
- 维护工具

### 第 4 期：高级体验

#### 目标

进一步提升效率和维护体验。

#### 范围

- 一键从当前工作流覆盖更新组
- 组间差异对比
- 批量搜索替换目标节点
- 预设模板
- 与提示词/模型资产管理联动

#### 产出物

- 高级编辑体验
- 工作流维护辅助工具

---

## 19. MVP 范围定义

### 19.1 推荐范围

第一阶段严格控制范围：

- 仅支持可序列化 widget 值
- 不做节点状态控制
- 不做改连线
- 不做自动重绑定
- 不做复杂工作流拓扑操作

### 19.2 原因

- 最快见效
- 风险最可控
- 最易验证 UI/API 一致性
- 最适合当前项目逐步迭代

---

## 20. 不建议第一期做的内容

- 图片/模型/latent 等复杂对象保存
- 改节点连线
- 改节点拓扑结构
- 深度依赖节点标题自动匹配
- 跨工作流自动映射
- 完整状态系统联动

这些内容复杂度高，且很容易影响系统稳定性。

---

## 21. 推荐文件规划

### 21.1 后端节点

- `nodes/workflow_group_preset_manager.py`

### 21.2 前端

- `web/js/workflow_group_preset_manager.js`

如果 UI 较大，建议拆分目录：

- `web/js/workflow_group_manager/`

### 21.3 路由

建议在 [routes.py](file:///d:/AI/comfyui/ComfyUI_windows_my/ComfyUI/custom_nodes/A_my_nodes/routes.py) 中新增专用接口。

### 21.4 存储

- `workflow_groups_db.json`

### 21.5 注册

在 [__init__.py](file:///d:/AI/comfyui/ComfyUI_windows_my/ComfyUI/custom_nodes/A_my_nodes/__init__.py) 中增加节点注册映射。

---

## 22. 开发任务拆分建议

### 22.1 第 1 期开发顺序

1. 定义 JSON 数据结构
2. 增加后端读取/保存路由
3. 新建控制节点骨架
4. 实现运行时 prompt 组选值应用
5. 实现前端组管理基础 UI
6. 实现“读取当前值保存为组”
7. 实现“应用当前组到画布”
8. 联调 UI 与 API

### 22.2 第 2 期开发顺序

1. 研究前端节点状态切换方式
2. 增加组项中的状态字段编辑
3. 切组时应用状态
4. 研究 API 模式状态同步语义
5. 增加错误提示

### 22.3 第 3 期开发顺序

1. 增加失效检测
2. 增加重绑定辅助
3. 增加导入导出
4. 增加版本与差异预览

---

## 23. 验收标准

### 23.1 第 1 期验收标准

满足以下条件视为通过：

- 可以创建多个组
- 每个组可保存多个节点输入值
- 可以切换组并更新前端节点值
- API 模式运行时能应用组选值
- 缺失项能给出明确提示
- 数据可持久化保存并重新加载

### 23.2 第 2 期验收标准

- 可以为组项设置状态
- 可以切换状态
- 状态应用失败有提示
- 常见节点不因状态切换直接破图

### 23.3 第 3 期验收标准

- 失效项可检测
- 支持重绑定
- 导入导出可用
- 分组维护成本明显下降

---

## 24. 测试建议

### 24.1 第 1 期最低测试集

- 单组单节点单值切换
- 单组多节点多值切换
- 多组来回切换
- UI 模式切组后运行正确
- API 模式执行按组值改写
- 删除组后 UI 同步正确

### 24.2 第 2 期增加测试

- `normal / bypass / disabled` 切换
- 状态异常提示
- 节点状态恢复

### 24.3 第 3 期增加测试

- 节点失效后重绑定
- 导入导出一致性
- 组版本更新与缓存行为

---

## 25. 风险评估

### 25.1 低风险

- 分组存储
- 组管理 UI
- 可序列化 widget 值批量切换

### 25.2 中风险

- API 模式 prompt 改写
- 值类型一致性
- 缓存失效策略

### 25.3 高风险

- 节点 `bypass`
- 节点 `disabled`
- 跨工作流重绑定
- 改连线或改拓扑

---

## 26. 最终建议

### 26.1 推荐实施顺序

建议按如下顺序推进：

1. 先做第 1 期 MVP
2. 第 1 期稳定后做第 2 期状态控制
3. 再做第 3 期稳定性增强
4. 最后做第 4 期高级体验

### 26.2 核心判断

这个想法是可行的，而且非常值得做。

但正确姿势不是：

- 一次性做一个全能节点

而是：

- 分阶段做一套可维护的系统

### 26.3 当前最优下一步

当前最适合继续输出的是：

- 第 1 期 MVP 的详细技术设计文档

建议下一版文档进一步细化：

- 控制节点字段定义
- JSON 精确 schema
- 前端 UI 交互流程
- 路由请求与响应格式
- prompt 改写细节
- 文件拆分计划
- 实现清单与优先级
